# Dev: leave VITE_CHATBOT_API_BASE empty — Vite proxies /api to Express (default http://localhost:3000).
# To call the API without proxy: VITE_CHATBOT_API_BASE=http://localhost:3000
# If your API listens on another host/port in dev:
# VITE_PROXY_TARGET=http://127.0.0.1:4000

VITE_CHATBOT_API_BASE=
