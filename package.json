{
  "name": "aeromaverick-chatbot-api",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build --prefix admin_panel && node scripts/sync-admin-to-public.cjs",
    "start": "node server.js"
  }
}
